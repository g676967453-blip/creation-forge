# 仓库系统 — 功能需求文档

> 版本：v0.1 | 日期：2026-07-30 | 状态：策划中
> 对应策划案：[2026-07-30-仓库系统-功能策划案.html](./2026-07-30-仓库系统-功能策划案.html)
> GDD 依据：[开仙门-GDD-v3.0.md](../../01-愿景/开仙门-GDD-v3.0.md) §3

---

## 1. 功能概述

**一句话**：纯数值仓库。三种资源以 int 存储，桌面态底部资源栏常驻显示。无格子、无图标仓库界面、无挂件存储。

**详细描述**：EconomyManager（新建 Autoload）管理三种资源（灵气/女娲石/流派秘宝）的 int 数值。各系统通过 EconomyManager 的 add/spend 方法增减资源。桌面态底部半透明资源栏常驻显示当前数量。资源增减时数字即时刷新。V0.1 不做背包界面，挂件不入仓库。

---

## 2. 模块影响分析

```
需要新建的模块：
  1. EconomyManager（autoload/economy_manager.gd，Autoload）
     职责：资源存取/校验/变更信号发射
  2. ResourceBar（scenes/ui/resource_bar.tscn + scripts/ui/resource_bar.gd）
     职责：桌面态底部资源栏 UI（显示三种资源的图标+数字）

触及的现有模块：
  1. GameManager — 初始化时创建 EconomyManager 实例；状态切换时显隐 ResourceBar
  2. SaveManager — 存档时从 EconomyManager 读取资源数据；读档时写入

不需要动的模块：
  - MountainManager / WaveManager / Enemy相关 — 它们调用 EconomyManager API 即可
```

---

## 3. 触发条件与状态约束

| 维度 | 说明 |
|------|------|
| 触发方式（增减） | 各系统调用 EconomyManager API |
| 触发方式（显示） | DESKTOP_IDLE 状态下 ResourceBar 常驻显示 |
| 互斥状态（隐藏） | INTRO / MAIN_PEAK_SELECT / MAIN_MENU 状态下不显示资源栏 |
| 可重复性 | 增减可无限次 |

---

## 4. 数据定义

### 4.1 资源枚举

```gdscript
enum ResourceType {
    SPIRIT_ENERGY,   # 灵气
    NUWA_STONE,      # 女娲石
    SECRET_TREASURE  # 流派秘宝（需传 peak_id 区分）
}
```

### 4.2 数据存储

```gdscript
# EconomyManager 内部
var _spirit_energy: int = 0
var _nuwa_stone: int = 0
var _secret_treasures: Dictionary = {}  # { "peak_sword": 0, "peak_buddha": 0, ... }
```

### 4.3 是否需要新 CSV？

```
☑ 不需要新 CSV。
  灵气基础速率等数值常量写在 EconomyManager 顶部 const。
```

---

## 5. 信号契约

```
信号 1（新建）：resource_changed(type: String, new_value: int, delta: int)
  发射者：EconomyManager
  接收者：ResourceBar（更新显示）、GameManager（存档触发判断）
  时机：任何资源变更后发射
  参数：type — "energy"/"stone"/"treasure:peak_id"；new_value — 变更后数值；delta — 变化量（正+负）

信号 2（新建）：resource_insufficient(type: String, required: int, current: int)
  发射者：EconomyManager
  接收者：GameManager（用于弹提示）
  时机：spend 时余额不足
  参数：type/required/current
```

---

## 5.1 EconomyManager API 速查表（全项目统一引用）

> **EconomyManager 是项目中唯一的资源管理器 Autoload。** 其他规格文档中的 `Warehouse` 均为 EconomyManager 的旧称，已废弃。

### 资源增加

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `add_energy(amount: int)` | amount — 增加量 | void | 灵气增加，内部 clamp 到 0 以上 |
| `add_stone(amount: int)` | amount — 增加量 | void | 女娲石增加 |
| `add_treasure(peak_id: String, amount: int)` | peak_id — 山峰 ID；amount — 增加量 | void | 流派秘宝增加 |

### 资源消耗

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `spend_energy(amount: int)` | amount — 消耗量 | bool | 灵气消耗。余额不足→发射 resource_insufficient → 返回 false |
| `spend_stone(amount: int)` | amount — 消耗量 | bool | 女娲石消耗。余额不足→同上 |
| `spend_treasure(peak_id: String, amount: int)` | peak_id — 山峰 ID；amount — 消耗量 | bool | 流派秘宝消耗。余额不足→同上 |

### 查询

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `get_energy()` | — | int | 当前灵气 |
| `get_stone()` | — | int | 当前女娲石 |
| `get_treasure(peak_id: String)` | peak_id — 山峰 ID | int | 指定流派秘宝数量 |
| `get_resources()` | — | Dictionary | 全部资源快照（供存档使用） |

### 信号

| 信号 | 参数 | 时机 |
|------|------|------|
| `resource_changed(type, new_value, delta)` | type — "energy" / "stone" / "treasure:peak_id"；new_value — 变更后值；delta — 变化量 | 任何资源变更后 |
| `resource_insufficient(type, required, current)` | type / required / current | spend 余额不足时 |

### 在各规格中的标准调用方式

```
# 消耗灵气（如进入挑战/修复山峰）
EconomyManager.spend_energy(100)  → 返回 bool

# 增加灵气（挂机/离线结算/战斗奖励）
EconomyManager.add_energy(amount)

# 消耗秘宝（修复山峰）
EconomyManager.spend_treasure("peak_buddha", 1)

# 检查余额（UI 按钮状态判断）
EconomyManager.get_energy() >= 500
```

---


## 6. 逻辑流程

```
1. 增加资源：
   EconomyManager.add_energy(amount) / add_stone(amount) / add_treasure(peak_id, amount)
   → _value += amount（clamp 到 0 以上）
   → 发射 resource_changed(type, new_value, amount)

2. 消耗资源：
   EconomyManager.spend_energy(amount) / spend_stone(amount) / spend_treasure(peak_id, amount)
   → 检查余额
     → 足够 → _value -= amount → 发射 resource_changed
     → 不足 → 发射 resource_insufficient → 返回 false

3. 秘宝存取：
   EconomyManager.add_treasure("peak_sword", 1)
   → _secret_treasures["peak_sword"] += 1
   → 发射 resource_changed("treasure:peak_sword", new_value, 1)
```

---

## 7. UI 表现

| 状态 | 表现 |
|------|------|
| 资源栏（常态） | 桌面态底部，半透明深色底栏，横向排列 3 类资源。每类：圆形图标(28×28) + 数字(14px bold) + 标签(10px) |
| 资源栏（增减） | 数字即时跳变。V0.1 无动画，V0.2 做数字滚动动画。 |
| 资源栏（有秘宝时） | 显示拥有的秘宝种类（如剑峰秘宝×2）。不显示数量为 0 的秘宝 |
| 资源栏（无秘宝时） | 只显示灵气+女娲石，不显示秘宝区域 |
| 资源栏（战斗态） | 不显示。战斗中有独立的战斗 HUD（见战斗系统策划案） |
| 资源不足提示 | V0.1 按钮置灰+tooltip。V0.2 弹 Toast |

---

## 8. 边界条件与互斥

```
☑ 资源变为负数？→ spend 时余额不足拒绝操作。add 接收负值时 clamp 到 0。
☑ 灵气超过 int 上限？→ Godot int 为 64 位有符号，上限 9e18，远超实际需求。不加额外限制。
☑ 挂件捡到第 4 个？→ 旧挂件被替换消失，不触发仓库操作。
☑ 多系统同时修改同一资源？→ EconomyManager 方法加锁（mutex）或 GDScript 单线程无需加锁。
```

---

## 9. 三层归属

```
表现层：scenes/ui/resource_bar.tscn + scripts/ui/resource_bar.gd
逻辑层：autoload/economy_manager.gd
数据层：EconomyManager 内部 Dictionary（存档时序列化到 JSON）

跨层检查：✓ 表现层不直接读写存档，通过 EconomyManager API ✓ 逻辑层不操作 UI 节点 ✓ 数据层不通知 UI
```

---

## 10. 反模式自查

```
✓ 不制造上帝 Manager（EconomyManager 只管资源存取）
✓ 无硬编码路径
✓ 无循环信号依赖：EconomyManager → ResourceBar（单向）
✓ 无双轨数据
```

---

## 11. 验收标准

1. 启动游戏首次进入桌面态 → 资源栏显示灵气=0、女娲石=0
2. 挂机 60s → 灵气 +600（速率 10/s），资源栏数字即时刷新
3. 战斗胜利 → 女娲石 +1，资源栏数字更新
4. 修复剑峰（消耗灵气 500）→ 灵气 -500，数字实时减少
5. 灵气不足时点击修复 → 按钮置灰，操作被拒绝
6. 存档后再读档 → 资源数量与存档时一致
7. 离线收益结算 → 灵气增加离线量，资源栏显示
8. 资源栏在开场黑幕/器灵选择/主菜单中不显示
