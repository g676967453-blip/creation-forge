# 需求文档：选择器灵

> ⚠️ 本文档已归档。最新版本见 `../03-功能规格/选择器灵.md`

> **状态：[已实现] | 验证日期：2026-07-09**

基于《功能规划-选择器灵.md》转译，本份文档标注了代码细节，AI 可直接按此执行。

---

## 改动清单

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `scripts/ui/main_peak_select_panel.gd` | 修改 | 默认无选中、按钮禁用态 |
| `scripts/ui/settings_button.gd` | **新建** | 设置按钮脚本 |
| `scripts/managers/game_manager.gd` | 修改 | 接入设置按钮、显示逻辑 |
| `scenes/main.tscn` | 修改 | 添加 SettingsButton 节点 |

---

## 改动 1：main_peak_select_panel.gd

### 1.1 默认无选中

将第 27 行：
```gdscript
var _selected_profile_id: String = "form_sword_01"
```
改为：
```gdscript
var _selected_profile_id: String = ""
```

### 1.2 移除 _ready 中的默认选中调用

将第 32-34 行：
```gdscript
func _ready() -> void:
    _build_ui()
    _update_selection(_selected_profile_id)
```
改为：
```gdscript
func _ready() -> void:
    _build_ui()
```

### 1.3 开始按钮初始禁用

在 `_build_ui()` 中，`_start_btn` 创建后（约第 147 行后）增加一行：

```gdscript
_start_btn.disabled = true
```

### 1.4 _update_selection 处理空选中

在 `_update_selection` 函数开头增加空值处理：

```gdscript
func _update_selection(profile_id: String) -> void:
    _selected_profile_id = profile_id
    if profile_id.is_empty():
        for card in _option_cards:
            card.modulate = Color(1.0, 1.0, 1.0)
        if _start_btn:
            _start_btn.disabled = true
        return
    # ... 后面保持原有的高亮 + 启用按钮逻辑
```

并在设置完高亮后增加启用按钮（在 `_on_option_pressed` 调用 `_update_selection` 时生效）：

原有代码中 `_update_selection` 只设置了 modulate，未管理按钮 disabled。改为末尾增加：

```gdscript
    if _start_btn:
        _start_btn.disabled = false
```

（以上插入到 `_update_selection` 尾部，在 `_option_cards[i].modulate` 赋值之后）

### 1.5 _on_start_pressed 加保护

在第 171 行：
```gdscript
func _on_start_pressed() -> void:
    start_game_requested.emit(_selected_profile_id)
```
改为：
```gdscript
func _on_start_pressed() -> void:
    if _selected_profile_id.is_empty():
        return
    start_game_requested.emit(_selected_profile_id)
```

---

## 改动 2：新建 settings_button.gd

在 `scripts/ui/` 下新建文件 `settings_button.gd`，内容：

```gdscript
extends Control
## 左下角设置按钮，点击返回器灵选择

signal pressed


func _ready() -> void:
    _build_ui()


func _build_ui() -> void:
    anchors_preset = Control.PRESET_BOTTOM_LEFT
    offset_left = 24
    offset_top = -84
    offset_right = 84
    offset_bottom = -24
    mouse_filter = Control.MOUSE_FILTER_STOP

    var btn := Button.new()
    btn.anchor_left = 0.0
    btn.anchor_top = 0.0
    btn.anchor_right = 1.0
    btn.anchor_bottom = 1.0
    btn.text = "设置"
    btn.custom_minimum_size = Vector2(60, 60)
    btn.pressed.connect(func():
        pressed.emit()
    )
    add_child(btn)
```

---

## 改动 3：game_manager.gd

### 3.1 添加设置按钮引用

在 `var` 声明区域（约第 28 行附近）增加：

```gdscript
var settings_button_ui: Control
```

### 3.2 _ready 中获取并连接设置按钮

在 `_ready()` 中的节点获取区域（约第 39 行后）增加：

```gdscript
settings_button_ui = root.get_node_or_null("UI/SettingsButton")
```

在信号连接区域（约第 53 行后）增加：

```gdscript
if settings_button_ui and settings_button_ui.has_signal("pressed"):
    settings_button_ui.pressed.connect(restart_game)
```

### 3.3 _set_state 中控制设置按钮可见性

在 `_set_state()` 中（约第 79 行后）增加：

```gdscript
if settings_button_ui:
    settings_button_ui.visible = (new_state == GameState.PREPARATION or new_state == GameState.BATTLE)
```

---

## 改动 4：main.tscn

### 4.1 添加 SettingsButton 节点

在 `UI` 节点下（约第 86 行前，ResultPanel 之前或之后均可）增加：

```
[node name="SettingsButton" type="Control" parent="UI"]
visible = false
layout_mode = 3
anchors_preset = 0
script = ExtResource("14")
```

同时需要在文件顶部的 `[ext_resource]` 区域增加一行（编号按实际顺延）：

```
[ext_resource type="Script" path="res://scripts/ui/settings_button.gd" id="14"]
```

---

## 模块影响分析

```
触及的现有模块：
  1. MainPeakSelectPanel/scripts/ui/main_peak_select_panel.gd — 默认无选中、按钮禁用、选中保护
  2. SettingsButton/scripts/ui/settings_button.gd — 新建设置按钮脚本
  3. GameManager/game_manager.gd — 接入设置按钮、显示逻辑、场景重置
  4. scenes/main.tscn — 添加 SettingsButton 节点

需要新建的模块：
  （无——SettingsButton 虽标注"新建"，但已在代码中实现）

不需要动的模块：
  - MountainManager — 经营阶段系统，选择器灵时不涉及
  - 所有战斗模块 — 选择器灵在战斗前
```

## 信号契约

```
新信号（已实现）：

start_game_requested(profile_id: String)
  发射者：MainPeakSelectPanel
  接收者：GameManager
  时机：玩家选中卡片后点击"开始游戏"
  参数：profile_id — 选中的 form_id（如 "form_sword_01"）

SettingsButton.pressed()
  发射者：SettingsButton
  接收者：GameManager.restart_game()
  时机：玩家点击左下角"设置"按钮
```

## 三层归属自查

```
表现层：
  - scenes/ui/（main_peak_select_panel 场景 + settings_button 场景）
  - scripts/ui/main_peak_select_panel.gd：卡片 UI + 交互
  - scripts/ui/settings_button.gd：设置按钮

逻辑层：
  - scripts/managers/game_manager.gd：接收信号、状态管理、restart_game()

数据层：
  - DataManager.peak_database：提供 6 座山峰的 icon_path 和 entry_form_id
  - peak_config.csv：山峰展示数据

跨层检查：
  ☑ 表现层代码是否直接读了 CSV？             → 否，通过 game_manager 传入数据
  ☑ 逻辑层代码是否直接操作了 UI 节点？        → 否，通过信号转发
  ☑ 数据层代码是否发射了信号给 UI？           → 否
```

## 反模式自查

```
☑ 这个功能是否在制造"上帝 Manager"？
  → 否。MainPeakSelectPanel 和 SettingsButton 各司其职

☑ 是否有新的硬编码 res:// 或 get_node("路径")？
  → 是（已知）。SettingsButton 在 main.tscn 中通过 ext_resource 引用，路径集中管理

☑ 是否在逻辑代码里直接操作了 UI？
  → 否

☑ 是否有数据在代码和 CSV 中双轨存在？
  → 否。山峰数据来自 peak_config.csv

☑ 模块之间是否有循环信号依赖？
  → 否
```

## 验收对照

| # | 规划文档验收标准 | 实现方式 |
|---|-----------------|---------|
| 1 | 三张卡片均未选中，"开始游戏"置灰 | `_selected_profile_id = ""` + `_start_btn.disabled = true` |
| 2 | 点击卡片 → 金色高亮，按钮可点击 | `_update_selection` 中管理 modulate + disabled |
| 3 | 切换高亮，始终只有一张 | 已有逻辑，无需改动 |
| 4 | 点击"开始游戏" → 面板消失，器灵图标切换 | 已有逻辑，无需改动 |
| 5 | 经营阶段左下角出现设置按钮 | `_set_state` 中管理 settings_button_ui.visible |
| 6 | 经营阶段点击设置 → 回到待机状态 | `restart_game()` → `reload_current_scene()` |
| 7 | 战斗阶段点击设置 → 回到待机状态 | 同上，BATTLE 状态下 visible=true |
