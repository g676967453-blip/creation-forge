# 项目待办总清单

> 来源：架构审计报告 + 技能/数值设计 + 美术任务规划
> 日期：2026-06-30（更新于 2026-07-02）
> 涵盖：第一阶段（架构修复）、第二阶段（数值设计）、第三阶段（美术资产）
> 新增：雷霆诀（第 7 功法）已实现，不在原始审计范围内

---

## P0 — 数据安全与运行时正确性（29 项，全部完成）

### S3: FormConfig Resource 永久污染

- [x] `main_peak.gd:131` — `range_up` 卡牌改为在 channel Dictionary 中存储 range_multiplier，不修改 `cfg.range`
- [x] `main_peak.gd:133-134` — `hp_up` 卡牌改为在 channel Dictionary 中存储 hp_multiplier，不修改 `max_hp`
- [x] `main_peak.gd:set_attack_channels()` — 添加 `"range_multiplier": 1.0` 到 channel 初始化
- [x] `main_peak.gd:_perform_sector_attack()` — 使用 `config.range * channel.get("range_multiplier", 1.0)`
- [x] `main_peak.gd:_ready()` — 设置 `_base_max_hp = cfg.max_hp`

### S4: 跨层直接 get_node() 调用 → 信号化（7 处）

- [x] `wave_manager.gd:137` — `gm.add_exp()` → 发射信号 `enemy_killed(exp_reward)`，GameManager 连接
- [x] `card_manager.gd:72` — `main_peak.apply_upgrade()` → 发射信号 `card_applied(form_id, effect_type, effect_value)`，GameManager 转发
- [x] `card_manager.gd:85` — `mountain_manager.get_all_activated_form_ids()` → 由 GameManager 在调用 `apply_card()` 时传入 active_form_ids 参数
- [x] `mountain_manager.gd:197,255` — `_game_manager.try_spend_spirit()` → 已重构：game_manager 在调用 mountain_manager 前自行处理灵气消费，mountain_manager 不再持有 game_manager 引用
- [x] `mountain_manager.gd:283` — `_game_manager.on_selected_mountain_changed()` → 已删除（与 `mountain_state_changed` 信号冗余），`on_selected_mountain_changed()` 方法已移除
- [x] `mountain_manager.gd:118-119` — `gm.on_mountain_deselected()` → 发射信号 `mountain_deselected`
- [x] `repair_prompt_panel.gd:244,250` — `gm.repair_selected_mountain()` / `upgrade_selected_mountain()` → 发射信号 `repair_requested` / `upgrade_requested`
- [x] `result_panel.gd:85` — `gm.return_to_preparation()` → 发射信号 `return_requested`

### S7: 召唤物跨层状态写入

- [x] `summon_artifact_sword.gd:26` — `ch["energy"]` 直接写入 → 改为信号 `energy_gained(amount)` 回传，MainPeak 连接 `_add_energy`
- [x] `summon_fox.gd:27` — 同上
- [x] `main_peak.gd:_create_fox()` — 移除 `set_meta("channel", channel)`，添加 `fox.energy_gained.connect(func(amt): _add_energy(channel, amt))`
- [x] `main_peak.gd:_create_sword_artifact()` — 同上

### S5: 死代码清理

- [x] `game_manager.gd:33` — 删除 `_operation_mode`
- [x] `game_manager.gd:35` — 删除 `_spirit_profile`
- [x] `game_manager.gd:267` — 删除 `on_selected_mountain_changed()`（已无调用者）
- [x] `wave_manager.gd:20` — 删除 `battle_ready`
- [x] `mountain_manager.gd:214` — 删除 `pre_repair_peak()`
- [x] `main_peak.gd:30` — 删除 `_sector_attack_queue`
- [x] `main_peak.gd:420` — 删除 `add_energy_to_first_channel()`
- [x] `main_peak.gd:586-605` — 删除 `enable_smite()` / `_perform_smite()` / `_smite_enabled` / `_smite_damage` / `_smite_timer`
- [x] `enemy_base.gd:203-211` — 删除 `apply_poison_stack()` / `_process_poison_stacks()`
- [x] `enemy_base.gd:209` — 删除 `get_poison_stacks()`
- [x] `enemy_base.gd:227` — 删除 `get_soul_marks()`
- [x] `enemy_base.gd:241-252` — 删除 `apply_slow()` / `apply_freeze()`

---

## P1 — 架构合规（20 项，10 已完成）

### S1: game_manager.gd 拆分（495 行 → 4 文件）

- [ ] 提取 `EconomyManager` — 灵气获取（click/auto）、灵气消费（try_spend）、EXP 累加和升级触发
- [ ] 提取 `BattleFlowController` — 战斗流程（倒计时→开始→波次桥接→结束→结算→返回经营）
- [ ] 提取 `UIStateCoordinator` — UI 状态机（各面板显隐控制、信号驱动）
- [ ] 保留 `GameManager` — 顶层编排 + 信号中继（≤150 行）

### S2: Profile 数据 CSV 化

- [x] 新建 `data/spirit_profile_config.csv` — 列：profile_id / profile_name / desc / icon_path / spirit_bonus / damage_bonus / exp_bonus / hp_bonus / start_level / start_exp_to_next
- [x] 新建 `scripts/data/spirit_profile_config.gd` — Resource 类
- [x] `data_manager.gd` — 增加 `_load_spirit_profiles()` 和 `get_spirit_profile(id)` / `get_all_spirit_profiles()`
- [x] `game_manager.gd:_on_start_game_requested()` — 从 DataManager 读取 Profile 数据，删除硬编码 match
- [x] `main_peak_select_panel.gd` — 删除硬编码 OPTIONS 数组，从 DataManager 读取

### S6: 表现层代码归位

- [x] `game_manager.gd:68-81` — Label 动态创建 → 已移除 fallback 创建逻辑，依赖 .tscn 中的 CountdownLabel 节点
- [ ] `mountain_manager.gd:62-98` — Area2D/CollisionShape2D 动态创建 → 移入 `.tscn` 场景或独立 `MountainClickAreaFactory`
- [ ] `mountain_manager.gd:273-278` — `sprite.modulate` 直接设置 → 发射信号由 Sprite 组件自行响应

### A1: 山峰升级生效

- [x] `main_peak.gd` — 弹道和扇形攻击伤害引入 `(1.0 + (level - 1) * 0.1)` 等级系数
- [x] `main_peak.gd:set_attack_channels()` — 接受 levels Dictionary 参数，存储在 channel["level"]
- [x] `mountain_manager.gd` — 新增 `get_peak_level(peak_id)` 方法
- [x] `game_manager.gd:start_battle()` — 构建 form_id→level 映射传入 main_peak

### A4: UI 构建函数拆分

- [ ] `main_peak_select_panel.gd:_build_ui()` (113行) → 拆分为 `_build_overlay()` / `_build_option_card()` / `_build_start_button()`
- [ ] `repair_prompt_panel.gd:_build_ui()` (110行) → 同上策略
- [ ] `card_selection_panel.gd:_build()` (99行) → 拆分为 `_build_overlay()` / `_build_card_slot()` / `_build_panel()`
- [ ] `repair_prompt_panel.gd:_refresh()` (71行) → 拆分为 `_refresh_technique_info()` / `_refresh_status()` / `_refresh_buttons()`

---

## P2 — 设计补完与代码质量（29 项，2 已完成）

### A2: 防御机制实装

- [ ] `main_peak.gd` / `enemy_base.gd` — 战斗中检查 `defense_type` 并应用对应逻辑
  - [ ] shield: 伤害先扣护盾，护盾归零后扣 HP
  - [ ] evasion: 按概率闪避（`randf() < defense_value`）
  - [ ] lifesteal: 造成伤害时回复 `damage * defense_value` HP
  - [ ] block: 固定减伤 `max(0, damage - defense_value)`

### A3: speed_up 卡牌修正

- [ ] `main_peak.gd:127-128` — `speed_up` 改为实际修改 `attack_interval`（乘以 `1.0 / (1.0 + effect_value)`），而非加 damage_multiplier
- [ ] 或保持实现不变，更新卡牌描述为"伤害 +7.5%"

### A5: UI 数据迁入 CSV

- [x] 新建 `data/spirit_profile_config.csv`（与 S2 合并，已于 6/30 完成）
- [x] `main_peak_select_panel.gd:6-25` — 删除 `OPTIONS` 硬编码数组，改为从 DataManager 读取（已于 6/30 完成）

### A6: summon_*.gd 元数据现代化

- [ ] `summon_artifact_sword.gd` — 用 `@export var` 替代全部 `get_meta`/`set_meta`
- [ ] `summon_fox.gd` — 同上

### A7: Resource 类工厂方法

- [ ] `card_data.gd` — 新增 `static func from_csv_row(row: Dictionary) -> CardData`
- [ ] `enemy_data.gd` — 同上
- [ ] `form_config.gd` — 同上
- [ ] `main_peak_config.gd` — 同上
- [ ] `peak_config.gd` — 同上
- [ ] `wave_data.gd` + `wave_enemy_group.gd` — 同上
- [ ] `data_manager.gd` — 5 个 `_load_*()` 方法改为委托给各自的 `from_csv_row()`

### A8: 调试快捷键守卫

- [ ] `game_manager.gd:461-468` — F1/F2/F3 包裹 `if OS.is_debug_build():`

### B 类逐项

- [ ] `projectile.gd` — 分离 `_draw()` 到子节点，用 typed property 替代 `get_meta`
- [ ] `main_peak.gd` — `_create_fox()` / `_create_sword_artifact()` 改为实例化 `.tscn` 场景
- [ ] `main_peak.gd` — stun 参数、大招参数硬编码 → CSV
- [ ] `enemy_base.gd` — `target_position`、`_attack_timer`、soul_mark 系数 → CSV
- [ ] `enemy_base.gd` — `ENEMY_SPRITES` 字典 → `enemy_sprite_config.csv`
- [ ] `enemy_data.gd:9` — 注释补充 `boss` 类型
- [ ] `wave_data.gd:5` — `Array` → `Array[WaveEnemyGroup]`
- [ ] `sector_effect.gd` — 属性赋值改为 `@export var`
- [ ] `result_panel.gd:68-72` — 变量声明移到文件顶部
- [ ] `card_selection_panel.gd:121-123` — 缓存子节点引用
- [ ] `mountain_manager.gd:53-60` — mapping 字典 → CSV

---

## 第二阶段：技能与怪物数值设计（51 项）

### 2-1 功法数值打磨（6 功法逐项）

#### 御剑诀（form_sword_01）— 单体飞剑 / combo

- [ ] 确认当前数值：dmg=18、projectile_count=1、interval=0.85s、range=600、combo 每 5 发穿透一次
- [ ] 评估：1 发弹道 DPS 偏低（18/0.85s≈21.2），combo 穿透触发频率是否够快？
- [ ] 评估：ultimate 30 发 ×1.5x 伤害是否合理，是否应随 level 缩放
- [ ] 设计 combo 机制的可视化反馈（combo 计数 HUD、穿透闪光）

#### 金刚咒（form_buddha_01）— 扇形 AOE / stun

- [ ] 确认当前数值：dmg=18、angle=90°、range=180、interval=1.0s、stun 12%概率 0.5s
- [ ] 评估：扇形角度 90° vs 180° 体感差异，是否需要角度成长
- [ ] 评估：stun 概率 12% 是否过低（平均 8.3 次攻击触发 1 次），是否应设递增概率
- [ ] 设计 stun 的视觉反馈（眩晕星星已有，确认效果清晰度）

#### 离火诀（form_magic_01）— 灼烧 DOT / explode

- [ ] 确认当前数值：dmg=10、projectile_count=4、interval=1.0s、range=550、burn 8 max stacks
- [ ] 评估：4 发 ×10dmg vs 御剑诀 1 发 ×18dmg，多目标 vs 单体的定位差异是否清晰
- [ ] 设计 burn 叠层机制：当前每层 DPS 多少？叠满 8 层的总 DOT 伤害是否够强？
- [ ] 评估：ultimate detonate（引爆全部灼烧层数）的数值，爆炸伤害系数是否合理
- [ ] 设计灼烧的视觉反馈（敌人着火特效/变色）

#### 炼魂诀（form_demon_01）— 灵魂印记 / weaken

- [ ] 确认当前数值：dmg=12、projectile_count=2、interval=0.9s、range=520、soul_mark 6%/层
- [ ] 评估：soul_mark 削弱 6%/层是否太弱（3 层才 18%），叠加到几层能感受到差异？
- [ ] 评估：ultimate 施加 3 层 + 引爆，引爆伤害 `20 * stacks` 是否合理
- [ ] 设计 soul_mark 的视觉反馈（敌人身上的印记图标/光环）

#### 灵狐契（form_beast_01）— 召唤灵狐

- [ ] 确认当前数值：fox_hp=120、fox_atk=8、fox_speed=180、1 只初始
- [ ] 评估：灵狐 DPS 8/0.9s≈8.9，对比御剑诀 21.2，召唤物的价值是否在"额外肉盾"而非输出？
- [ ] 评估：灵狐 revive 6s 是否过短/过长，复活后是否该有短暂无敌
- [ ] 设计灵狐的视觉反馈（移动轨迹、攻击动画、死亡/复活特效）

#### 御器诀（form_artifact_01）— 召唤法器飞剑

- [ ] 确认当前数值：sword_atk=10、orbit_radius=80、orbit_speed=2.5、1 把初始
- [ ] 评估：法器飞剑的 DPS 和灵狐对比，两者定位差异（环绕防御 vs 主动出击）
- [ ] 评估：orbit_radius=80 在 200 探测范围内是否有足够覆盖率
- [ ] 设计法器飞剑的视觉反馈（旋转轨迹光环、攻击时闪光）

### 2-2 卡牌数值平衡

- [ ] 统计 28 张卡牌的效果值与稀有度分布
- [ ] 检查：普通卡（common）是否均可多次选取叠加？最大叠加次数是否合理？
- [ ] 检查：稀有卡（rare）是否真的"一旦出现就被排除"？池耗尽后的重置逻辑是否正确
- [ ] 评估：通用卡（damage/speed/range/hp）vs 功法专属卡的吸引力平衡
- [ ] 修正 `speed_up` 卡牌 — 改实现（改 attack_interval）或改描述
- [ ] 补充卡牌：每个功法至少 4 张专属卡是否够？是否需要补到 6 张（3 common + 3 rare）

### 2-3 大招（Ultimate）参数标准化

- [ ] 建立大招数值公式：伤害系数 × 功法基础伤害 × channel 倍率
- [ ] 当前 6 个大招参数对比表：
  - 御剑诀：30 发 ×1.5x
  - 金刚咒：360° ×2.0x + 1.2s stun
  - 离火诀：引爆全部 burn × 系数
  - 炼魂诀：3 mark + detonate 20dmg×stacks
  - 灵狐契：2x dmg + invincible 8s
  - 御器诀：2x dmg + 2x speed 8s
- [ ] 统一能量获取速率（energy_per_hit），确保不同功法攒大招的时间相近
- [ ] 大招参数全部迁入 `form_config.csv` 或新建 `ultimate_config.csv`

### 2-4 怪物行为设计（4 种敌人差异化）

#### 疾冲魔（enemy_rush）— 高速炮灰

- [ ] 确认当前：HP=50、speed=60、damage=10、size=0.8、path=straight
- [ ] 设计特殊行为：成群出现时是否有加速光环？到达器灵后自爆？

#### 侧袭者（enemy_flank）— 侧翼包抄

- [ ] 确认当前：HP=80、speed=50、damage=12、size=1.0、path=left_flank
- [ ] 设计特殊行为：实际走曲线路径而非直线？从左右两侧交替出现？
- [ ] 当前 left_flank 路径已实现但代码中 right_flank 未使用 — 是否启用交替侧袭？

#### 巨兽（enemy_giant）— 重装坦克

- [ ] 确认当前：HP=300、speed=20、damage=25、size=2.0、path=straight
- [ ] 设计特殊行为：周期性冲锋（短暂加速）、到达器灵后 AOE 震荡波？
- [ ] 评估：size=2.0 的碰撞体积是否影响路径和拥挤行为

#### 天魔 BOSS（enemy_boss）— 终极威胁

- [ ] 确认当前：HP=800、speed=30、damage=40、size=3.5、path=straight
- [ ] 设计特殊行为：阶段切换（50% HP 进入狂暴）、召唤小怪、全屏 AOE？
- [ ] 设计 BOSS 出场效果（屏幕震动、警告文字、特殊入场动画）
- [ ] BOSS 是否需要专属掉落/奖励？

### 2-5 波次节奏设计

- [ ] 当前 10 波配置审查（wave_config.csv 26 行）
- [ ] 设计关键波次节奏：
  - 波 1-2：教学波（少量疾冲魔，让玩家熟悉操作）
  - 波 3-4：引入侧袭者（难度爬升）
  - 波 5：中场休息波（少量敌人 + 更多经验）
  - 波 6-7：引入巨兽（混合编队压力测试）
  - 波 8-9：高强度混合波（所有敌人类型）
  - 波 10：BOSS 战（天魔 + 少量护卫）
- [ ] 波间是否需要短暂停顿（当前是无缝连续出怪）
- [ ] 波次数量：10 波是否合适？是否需要扩展到 15 或 20 波？

---

## 第三阶段：美术资产任务规划（32 项）

### 3-1 敌人精灵（4 项）

- [ ] 疾冲魔（enemy_rush）— 已有 `enemy_rush_sheet.png`，确认帧动画完整
- [ ] 侧袭者（enemy_flank）— **缺失**：需设计精灵表（建议 4-6 帧，蓝/紫色调，侧向移动姿态）
- [ ] 巨兽（enemy_giant）— **缺失**：需设计精灵表（建议 6-8 帧，大型厚重体型，紫色/暗色）
- [ ] 天魔 BOSS（enemy_boss）— 已有 `boss_dragon_sheet.png`（6 帧 70×73），确认入场动画是否需要额外帧

### 3-2 投射物精灵（6 项）

- [ ] 御剑诀飞剑 — 已有 `sword_projectile.png`（23×49），可考虑增加旋转帧
- [ ] 金刚咒拳形 — **缺失**：需设计拳/掌形投射物（金色，光晕效果）
- [ ] 离火诀火球 — **缺失**：需设计火球精灵（橙红色，带拖尾）
- [ ] 炼魂诀魂火 — **缺失**：需设计灵魂火焰精灵（紫色，半透明，飘动感）
- [ ] 灵狐契 — 召唤物而非投射物，灵狐需独立精灵（见 3-3）
- [ ] 御器诀 — 召唤物而非投射物，法器飞剑需独立精灵（见 3-3）

### 3-3 召唤物精灵（2 项）

- [ ] 灵狐 — **缺失**：需设计灵狐精灵表（建议 4-6 帧，白色/淡金色，带灵气光晕）
- [ ] 法器飞剑 — **缺失**：需设计飞剑精灵（与御剑诀飞剑区分，更大更有质感，环绕旋转帧）

### 3-4 器灵与山峰（4 项）

- [ ] 器灵本体 — 当前纯代码绘制（紫色多边形），需设计器灵主体精灵
- [ ] 山峰图标 — 当前 3 张 PSD 导出（书/佛/鼎）被 6 峰重复使用，需为每峰设计独立图标：
  - [ ] 剑峰（peak_sword）— 剑修主题
  - [ ] 佛峰（peak_buddha）— 佛修主题
  - [ ] 法峰（peak_magic）— 御法主题
  - [ ] 魔峰（peak_demon）— 魔修主题
  - [ ] 兽峰（peak_beast）— 御兽主题
  - [ ] 宝峰（peak_artifact）— 炼宝主题
- [ ] 山峰修复/未修复状态 — 两张贴图切换（残破 vs 完好）

### 3-5 场景背景（2 项）

- [ ] 战场背景 — **缺失**：需设计云端/仙山背景（180° 扇形防御区，云海+远山）
- [ ] 经营阶段背景 — **缺失**：需设计器灵+山峰的经营界面背景

### 3-6 UI 贴图（6 项）

- [ ] 面板底框 — 当前全代码 StyleBoxFlat 绘制，需设计 9-patch 面板贴图（仙侠风格边框）
- [ ] 按钮贴图 — 普通/悬停/按下三态（带金属/玉石质感）
- [ ] 卡牌底板 — 卡牌选择界面的卡牌底框（按稀有度分色：白/蓝/紫/金）
- [ ] 器灵选择卡片 — 开局 3 张器灵卡片的底板
- [ ] HP/能量条 — 当前代码绘制，需设计带雕纹的条形贴图
- [ ] 图标集 — 灵气图标、经验图标、波次图标等小元素

### 3-7 特效（3 项）

- [ ] 命中特效 — 各功法命中敌人的差异化特效（剑痕/拳印/火焰/紫光）
- [ ] BOSS 出场特效 — 屏幕震动 + 暗红色光晕 + 文字警告
- [ ] 升级特效 — 战斗中获得卡牌时的金光特效

### 3-8 音效（1 项 — 后续细分）

- [ ] 音效/BGM 全部空白，需独立音效制作计划（攻击音/命中音/UI 点击/波次提示/胜利/失败/BGM）

---

## 进度追踪

| 阶段 | 总项 | 已完成 | 进度 |
|------|------|--------|------|
| P0 — 数据安全与运行时正确性 | 29 | 29 | 100% |
| P1 — 架构合规 | 20 | 10 | 50% |
| P2 — 设计补完与代码质量 | 29 | 2 | 7% |
| 第二阶段 — 技能与数值设计 | 51 | 0 | 0% |
| 第三阶段 — 美术资产 | 32 | 0 | 0% |
| **合计** | **161** | **41** | **25%** |

> 注：原始估算 111 项，展开后实际 161 个 checkbox。A5 的 2 项与 S2 重复，已于 6/30 完成。
> 雷霆诀（第 7 功法）的 12 个文件/场景不在原始审计范围内，未计入。
