# DSH 信息日报 · 2026-09-10

> 报告期：首期（基线） ｜ 生成方式：手动（首期） ｜ 数据源：本机文件系统 + npm registry + GitHub Releases
> 采集时间：2026-09-10 22:58 (+08:00)

本期是**首期基线报告**，重点是把「你现在到底跑在哪套 DSH 上」钉死，方便以后每期只讲变化。

---

## ① 本机 DSH 环境快照

| 项 | 值 |
| --- | --- |
| DSH CLI 版本 | **0.1.5-rc.1** |
| 安装路径 | `C:\Users\Administrator\AppData\Roaming\npm\node_modules\@deepseek-ai\dsh` |
| npm `latest` 标签 | 0.1.5-rc.1 —— **已是最新** |
| npm `next` 标签 | 0.1.5-rc.2 —— 存在更新的预发布版 |
| Node 运行时 | v24.15.0 |
| 系统 | win32 x64 · 10.0.26200 |
| DSH_HOME | `C:\Users\Administrator\.dsh` |
| 已启用 profile | `web`（bundles: `@deepseek-ai/dsh-base`、`@deepseek-ai/dsh-web-app`） |
| 默认模型 | provider `deepseek-official` · model **`deepseek-flash`** · reasoningEffort `low` |
| 模型凭据 | 已配置（仅检查存在性，不读取内容） |
| 活跃会话数 | 2 |
| 工作台插件 | `dsh-worktable` **0.3.0** |

**一句话结论**：DSH 本体不需要动；真正有更新价值的是 `dsh-worktable` 插件（0.3.0 → 0.3.3）。

---

## ② 更新与变更提醒

### 1. DSH 本体 —— 无需升级

当前 `0.1.5-rc.1` 就是 npm `latest`。另有 `next = 0.1.5-rc.2` 预发布版，属于官方灰度通道，**不建议**日常切过去。

### 2. `dsh-worktable` 0.3.0 → 0.3.3 —— 有更新，但升级要谨慎

上游三个版本的变更：

| 版本 | 发布 | 关键内容 |
| --- | --- | --- |
| v0.3.1 | 09-03 | 修复 v0.3.0 发布包为非标准 npm 格式、安装会报错的问题 |
| v0.3.2 | 09-05 | 文件夹选择显示「等待选择…」、新增「手动输入」绝对路径入口、失败显式报错 |
| v0.3.3 | 09-06 | 适配 DSH 0.1.2-rc.1 / 0.1.1-rc.2；**数据读取不再依赖默认安装路径**（自定义 `DSH_HOME` 也能读到项目与分组）；修复点击项目卡片无反应 |

⚠️ **升级前必须注意两点**

1. **仓库里的 worktable 是本地优化版**，不是原版 0.3.0。
   `造化仪表盘/tools/dsh-harness/plugins/dsh-worktable` 带有自加的「可编辑地址栏 / 右键文件操作 / `/api/worktable/rm|rename|copy|open-path`」等改动（见 dsh-harness README 的「本地优化摘要」）。
   直接拉上游 0.3.3 覆盖会**丢掉这些改动**。正确顺序：先 diff 上游 → 把本地改动 rebase 到 0.3.3 → 再替换。
2. **兼容性未被上游声明**。v0.3.3 官方只声明测过 DSH `0.1.2-rc.1` 与 `0.1.1-rc.2`，**没有**声明兼容你现在跑的 `0.1.5-rc.1`。而 0.1.5-rc.1 恰好改了 Web 插件面板 API（`sidebar.panellist`、`main`、原 `conversation` slot 迁移）。升级后必须实测再决定是否长期保留。

> **建议**：当前工作台既然能用，就不急着升。等确认要升时，按上面第 1 点的顺序做，升完实测「项目卡片点击 / 文件夹选择 / 资源管理器右键 / 控制台布局」四项。

---

## ③ 本周期 DSH 动态

### 发布节奏

| 日期 | 版本 | 说明 |
| --- | --- | --- |
| 09-04 | `dsh-v0.1.3-alpha.1` | Session 格式升 v2；`SessionHandle` 破坏性变更 |
| 09-07 | `dsh-v0.1.3-alpha.2` | 「在应用中打开」；子代理支持排队/编辑/Steer |
| 09-08 | `dsh-v0.1.5-alpha.1` | 动态系统提示词不破坏 KV Cache；实验性右侧 Sidebar |
| 09-09 | `dsh-v0.1.5-alpha.2` | Sidebar 文档预览（MD/代码/HTML/PDF/图片） |
| **09-10** | **`dsh-v0.1.5-rc.1`** ← 你已装 | 0.1.5 首个候选版，汇总自 0.1.2-rc.1 以来全部变更 |

### `0.1.5-rc.1` 里与你直接相关的几条

- **模型**：DeepSeek 适配器新增 `DeepSeek-V41-Flash`（`deepseek-flash`），支持文本、图片与会话历史中的系统提示词更新；新会话默认使用它。→ 你 settings 里钉的就是这个模型。
- **右侧 Sidebar**：多标签、分栏、全屏，支持 Markdown / 代码高亮 / HTML / PDF / 图片预览；模型可**显式交付文件**供预览、用默认应用打开、在文件管理器中定位。原 Detail 面板已移除。
- **任意类型文件上传**：文件与图片同一预览区混排，后台上传有进度、可取消、切换会话仍续显。
- **可继续子代理**：支持消息排队、编辑、删除、单条/全部 Steer、Stop。
- **动态系统提示词**：不破坏 KV Cache（需模型显式声明支持）。
- **代理支持**：所有出站请求遵循 `HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` / `NO_PROXY`。
- **Web 顶栏「在应用中打开」**：用已装的编辑器 / IDE / 终端 / 文件管理器打开 Workspace。
- **会话统计**：拆成两个可展开摘要——轮次与速度、精确 Token 用量与缓存命中。
- **Skill 选择器**支持模糊搜索；内置斜杠命令说明中文化。
- **`/feedback`** 可独立提交，不必继续对话。

### 需要注意的破坏性 / 行为变更

- **Session 数据格式升级至 V3**：旧日志会迁移为新文件并保留原文件，但**升级后的会话不支持降级读取**；自定义日志读取器需适配。→ 你的 `sessions/` 不在 git 同步范围内，风险可控，但**别在升级前把 DSH 降级回去**。
- **默认工具调整**：SDK、**Headless**、ACP 默认使用 `read` / `write` / `edit` 编辑文件。→ 这条对你是**好消息**：本日报的自动生成正是靠 headless 写文件。
- **插件 API 调整**：移除 `ctx.agent`；`Inbox` 改为类型接口；Web 插件面板注册方式变更（`sidebar.panellist` / `main`）。→ 自己写插件的场景要留意，也是 worktable 兼容性的不确定性来源。

来源：[v0.1.5-rc.1 release](https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.5-rc.1) · [v0.1.5-alpha.2](https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.5-alpha.2) · [v0.1.3-alpha.1](https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.3-alpha.1)

---

## ④ 使用技巧（偏向「怎么用」）

### 技巧 1 · 用右侧 Sidebar 当「第二屏」，别再切窗口

- **一句话**：DSH 现在能在右侧直接预览 HTML / PDF / 图片 / Markdown，还能多标签分栏。
- **怎么做**：把文件拖进会话，或让模型把产物「交付到 Sidebar」；HTML 报表（你的 `reports/*.html`）、美术规格 PDF、Godot 截图都能直接在旁边看。
- **对你的价值**：做美术排期和仪表盘时，不用再来回切换浏览器和资源管理器。

### 技巧 2 · 参考资料一律「拖进来」，不限类型

- **一句话**：Web 现在支持上传任意类型文件，后台上传有进度、可取消。
- **怎么做**：把 PDF 规格、PSD 说明、Godot 场景文件直接拖进输入框；模型可以用文件工具按路径按需读取。
- **对你的价值**：IAA / 概念设计工作流的参考图与文档可以一次性投喂，不用先转格式。

### 技巧 3 · 子代理跑着的时候，直接插话

- **一句话**：可继续对话的子代理支持排队、编辑、删除和 Steer。
- **怎么做**：子代理还在跑时继续发消息（进入队列），需要它转向就用 Steer，不需要就 Stop。
- **对你的价值**：游戏 / 美术 / 小红书几条线并行时，不用等一个子代理跑完才下一条指令。

### 技巧 4 · 养成看「会话统计」的习惯，把成本管住

- **一句话**：统计拆成了「轮次与速度」和「精确 Token 与缓存命中」两个摘要。
- **怎么做**：点开会话统计，重点看缓存命中率——命中高说明上下文前缀稳定，重复读文件、随便改系统提示词都会拉低它。
- **对你的价值**：日报这类周期性任务最容易悄悄烧 token，有数字才能判断要不要降级成 `-NoLlm`。

### 技巧 5 · 公司网络要代理时，写在 Harness-home 的 `.env` 里

- **一句话**：出站请求现在遵循 `HTTP_PROXY` / `HTTPS_PROXY` / `ALL_PROXY` / `NO_PROXY`。
- **怎么做**：写进 `$DSH_HOME/.env`。注意代理变量**只接受 Harness-home 的 `.env`**，不接受工作目录里的 `.env`（官方明确的安全边界）。
- **对你的价值**：公司/家里网络环境切换时，Web 搜索与模型调用不会莫名失败。

### 技巧 6 · 「在应用中打开」+ Skill 模糊搜索

- **一句话**：顶栏能把 Workspace 用已装应用打开；技能多了也能搜了。
- **怎么做**：用顶栏「在应用中打开」直接跳 VS Code / 终端 / 资源管理器；输入 `/` 后直接打关键词筛技能。
- **对你的价值**：你的 `.claude/skills` 下技能已经不少，模糊搜索省事。

---

## ⑤ 今天可以试一下

1. **验证 Sidebar 第二屏**：在本会话里让我把 `造化仪表盘/reports/造化坊仪表盘.html` 交付到右侧 Sidebar，看看分栏预览顺不顺手。
2. **看一眼缓存命中**：打开本会话的统计摘要，记下当前 Token 用量与缓存命中率，作为以后对比的基线。

---

## ⑥ 与本仓库的联动

- **本日报工具链**落在 `造化仪表盘/tools/dsh-report/`，产出在 `造化仪表盘/reports/dsh-daily/`，并被仪表盘 `/board/` 静态服务直接覆盖 —— 浏览器打开 `http://127.0.0.1:3456/board/dsh-daily/` 即可读。
- **`dsh-harness` 便携包**：worktable 的升级取舍见第 ② 节；便携包本身不涉及 Session 格式（`sessions/` 不同步），V3 升级不影响跨机器同步。
- **headless profile**：本日报的自动生成依赖 `dsh --profile headless`，而 0.1.5-rc.1 恰好把 headless 的默认文件工具定为 `read`/`write`/`edit`，方向一致。

---

## ⑦ 附录：数据源与失败项

**数据源**

- 本机文件系统：`~/.dsh/settings.yaml`、`~/.dsh/profiles/*/package.json`、`~/.dsh/sessions/`、仓库 `dsh-worktable` 包
- npm registry：`@deepseek-ai/dsh` 的 `dist-tags`
- GitHub Releases API：`deepseek-ai/deepseek-harness`、`Aisland-SJL/dsh-worktable`

**采集异常**

| 项 | 级别 | 说明 |
| --- | --- | --- |
| `git` | warn | 本期生成于受限沙箱内，git 子进程不可用，仓库分支信息降级为直接读 `.git/HEAD`（已取到 `main`）。正式的计划任务运行时无此限制。 |

**未采集项（有意）**

- 任何 API Key / `.credentials.yaml` 内容 —— 只报「是否已配置」的布尔值。
