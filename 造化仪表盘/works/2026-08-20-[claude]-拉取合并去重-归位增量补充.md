---
date: 2026-08-20
ai: claude
type: 系统维护
status: 完成
tags: [仓库卫生, 根目录归位, 拉取合并, 多AI协作]
---

# [2026-08-20] 拉取合并去重：归位增量补充

---

## 📋 问题解决日志

### 遇到了什么

拉取远程时发现另一 AI 会话（c63acb3）已同步完成根目录归位（game-bot/情景认知小程序入 projects、tmp 移出跟踪等），本地未提交的梳理变更与远程大面积重叠。`git pull` 前必须先处理本地变更。

### AI 怎么协作的

1. **stash 隔离**：将本地梳理变更整体 `git stash push -u`，干净工作区后再 pull，避免 rename 冲突
2. **逐项对比去重**：pull 后对照 `git stash show` 与远程 merge 结果，把变更分三类——
   - 远程已做（rename/tmp 删除）→ 丢弃
   - 两边冲突（CLAUDE.md / collect-data.ts / 仪表盘）→ 丢弃本地版，在远程新版上做增量
   - 本地独有（IAA买量归位、works 重命名、引用更新）→ 从 `stash@{0}` / `stash@{0}^3` 选择性恢复
3. **空壳清理**：远程把 game-bot 移入 projects/ 后，根目录残留含 ignore 产物的空壳目录，删除壳

### 产出结果

**本地独有增量**（远程未做，本次恢复）：
- `IAA买量数据模型认知.md` → `docs/zh-CN/`（与 IAA软著文档同库）
- `projects/情景认知小程序/project.json`（sortOrder 7，仪表盘动态加载）
- `works/2026-08-11-claude-ai-building-prompt-*.md` ×2 重命名补 `[claude]` 标签
- works 08-10/08-14 两处路径引用、`.workbuddy/memory/2026-08-10.md` 的 overview.md 路径更新

**远程版基础上的增量**：
- `CLAUDE.md`：结构声明补全 projects/ 全部 9 项目（含 projects/game-bot）
- `tools/collect-data.ts`：补「情景认知小程序」仪表盘条目（加锁操作）
- 仪表盘重新生成

**去重处理**：本地的 08-19 梳理日志与远程「根目录项目归位」日志内容重复，删除本地版（一事一记原则）

### 关联项目

全局（目录治理），涉及 projects/情景认知小程序、projects/game-bot、docs/zh-CN/
