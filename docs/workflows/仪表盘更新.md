---
name: 仪表盘更新
version: v2
skill: /update-dashboard
project: 全局
status: mature
category: 造化坊
desc: 重新生成造化坊 HTML 仪表盘（7 页签）
steps: 检查 造化仪表盘/tools/collect-data.ts → 更新过时数据 → 生成 HTML → 用户验证
trigger: /update-dashboard 或「更新仪表盘」
---

# 仪表盘更新

> 版本：v2 | 最后更新：2026-07-27 | 对应 SKILL：`/update-dashboard`

## 触发条件

- 用户说 `/update-dashboard` 或 "仪表盘更新" 或 "更新仪表盘"
- 项目结构或数据发生变更后

## 人机分工

| 步骤 | 人 | AI |
|------|-----|-----|
| 1. 检查来源 | — | 读取 `造化仪表盘/tools/collect-data.ts`，确认 8 个页签的数据与当前状态一致 |
| 2. 更新脚本 | 告知需要更新的内容 | 修改 `造化仪表盘/tools/collect-data.ts` 中的硬编码数据 |
| 3. 生成 | — | 执行 `npx tsx 造化仪表盘/tools/generate-dashboard.ts` |
| 4. 验证 | 打开 HTML 抽查各页签 | — |

## 执行步骤

### 1. 检查来源

AI 检查 `造化仪表盘/tools/collect-data.ts` 中的数据，确认与当前状态一致。仪表盘共 **8 个页签**：

| 页签 | 数据来源 | 检查内容 |
|------|---------|---------|
| 系统总览 | `collectData()` 动态采集 | 信条标语、6 统计卡片（SKILL 文件/工作流数/工具知识库/工作日志/Git 提交/活跃项目）、系统信息表（AI 模式/SKILL/项目结构/活跃项目数） |
| 项目状态 | `projects[]` 硬编码 | 5 个项目各字段：engine、status、progress、output、blocker（GAME-002/小红书/asset-pipeline/tutorial/originals） |
| 工作流清单 | `workflows[]` + `skills[]` | 11 个工作流（二级页签：全部/造化坊/自媒体/游戏开发/主美工作/SKILL仓库）；SKILL 文件（类型/难度/用途描述/关联工作流） |
| 近期动态 | `listWorks()` + `gitLog()` 自动采集 | 造化仪表盘/works/ 日志（文件日期+描述）、Git 提交历史（hash+message） |
| 个人待办 | `loadPersonalTasks()` 解析 `造化仪表盘/个人待办.md` | 5 分类个人任务（汇总卡片 + 分类筛选 + 任务表格 + 周度归档折叠），通过 `/todo` SKILL 管理。已合并旧「任务看板」页签的历史数据 |
| 目标计划 | `goalsUser[]` + `goalsIssues[]` + `goalsAI[]` 硬编码 | 用户需求（6 条）、系统问题（7 条诊断）、AI 建议（4 条） |
| 知识库 | `toolGuides[]` 硬编码 | 5 个工具（Git/GitHub/Pixso/Claude Code Skills/Claude Code 配置），每个含入门/操作/协作文档链接 |
| 资产地址 | `assets[]` + `external[]` 硬编码 | 11 个内部资产路径（系统层 6 + 项目层 4 + 外部 1）、3 个外部平台（Pixso/GitHub/小红书） |

### 2. 更新脚本

AI 更新 `造化仪表盘/tools/collect-data.ts` 中过时的数据，用户确认。需关注的硬编码数据：

- `projects[]` — 项目状态、进度、产出、阻塞项
- `workflows[]` — 工作流列表、版本、对应 SKILL（`skill` 字段）
- `tasks[]` — 任务看板
- `goalsUser[]` / `goalsIssues[]` / `goalsAI[]` — 目标计划
- `toolGuides[]` — 工具知识库
- `assets[]` / `external[]` — 资产地址

动态采集的数据（无需手动维护）：
- SKILL 文件统计（自动扫描 `.claude/skills/`）
- 工作日志（自动扫描 `造化仪表盘/works/`）
- Git 提交历史（`git log`）

### 3. 生成

```bash
npx tsx 造化仪表盘/tools/generate-dashboard.ts
# 输出 造化仪表盘/reports/造化坊仪表盘.html
```

### 4. 验证

用户在浏览器中打开 HTML，抽查各页签内容是否正确。

## 板块独立盘（2026-09-05 起，主盘之外）

板块2-5 各有独立生成式仪表盘，与主盘同跑于 `造化仪表盘/reports/`：

| 盘 | 产物 | 数据源 |
|----|------|--------|
| 板块2 项目库 | `板块2-项目库仪表盘.html` | 各项目 `project.json` + PROGRESS.md + git |
| 板块3 知识库 | `板块3-知识库仪表盘.html` | docs/ 子目录扫描 + 工作流 frontmatter |
| 板块4 美术产线 | `板块4-美术产线仪表盘.html` | `asset-pipeline/PROGRESS.md` + `dashboard-data.json`（人工数据权威源）+ 批次统计 |
| 板块5 小红书 | `板块5-小红书仪表盘.html` | xiaohongshu/ 期目录实物 + 每期 index.md + PROGRESS「发布统计」 |

- 生成器：`造化仪表盘/tools/board{2,3,4,5}-dashboard.ts`（每盘一个，collect+generate）+ 共享库 `board-dashboard-lib.ts`；编排器 `generate-board-dashboards.ts`
- 刷新：`npx tsx 造化仪表盘/tools/generate-board-dashboards.ts`（不带参数全量；带 `b2\|b3\|b4\|b5` 单盘）
- 板块盘**只读复用** collect-data.ts 已导出函数，主盘生成链路零改动；唯一人工数据源是 `asset-pipeline/dashboard-data.json`（b4），改后刷 `b4`
- 本机服务：dashboard-server 启动时自动全量刷新，`/board/` 静态可看，主盘侧栏底部注入「板块独立盘 · 本机模式」入口条（仅本机响应，GH Pages 静态产物不含）

## 产出物

- `造化仪表盘/reports/造化坊仪表盘.html`（主盘，独立 HTML 7 页签，浏览器打开即看）
- `造化仪表盘/reports/板块{2,3,4,5}-*.html`（板块独立盘，自包含单文件，file:// 直开）

## 约束与规则

- **数据来源是 `造化仪表盘/tools/collect-data.ts`**：HTML 是衍生文件，不应手动编辑
- **造化仪表盘/works/ 日志驱动近期动态**：新增 造化仪表盘/works/ 文件后自动出现在近期动态中
- **SKILL 文件自动扫描**：无需手动维护 SKILL 文件数量
- **生成后即用**：HTML 是独立文件，可直接在浏览器中查看，也可通过 `npm run dashboard` 启动本地服务器
