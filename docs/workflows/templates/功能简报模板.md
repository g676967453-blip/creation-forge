# 功能简报模板

> **定位**：独立游戏单人+AI 协作的策划文档。这份文档是 AI 执行的唯一输入源。
> **原则**：只写 AI 需要的信息。5 节，不可多。
> **使用**：人类读 3 分钟确认 → AI 三线并行开工。
> **流程**：[独立游戏-功能开发流程-v0.1](../独立游戏-功能开发流程-v0.1.md)

---

# [功能名称] — 功能简报

## 1. 一句话 + 流程位置

**一句话**：[20 字以内]

**流程位置**：
```
[前一个节点] → [本功能] → [下一个节点]
```

---

## 2. 界面与交互

### 界面元素
- [列出所有玩家能看到的东西：面板/卡片/按钮/文字/图标/槽位/条]

### 操作 → 反馈

| 操作 | 反馈 |
|------|------|
| [玩家的操作] | [系统的反馈（视觉变化+音效）] |
| [异常操作] | [错误反馈，不能静默失败] |

### 状态

| 状态 | 表现 |
|------|------|
| 默认态 | [刚进入界面时的样子] |
| 选中态 | [选中某个元素后的变化] |
| 禁用态 | [不可操作时的样子] |
| 空态 | [数据为空时的样子] |
| 完成态 | [操作完成后的过渡+跳转] |

---

## 3. 不在范围内的

```
本功能不包括：
  1. [明确排除项]
  2. [明确排除项]

后续版本考虑（但本版本不做）：
  1. [可以有但先不做的]
```

---

## 4. 涉及的数据

**涉及 CSV**：
| 文件 | 操作 | 说明 |
|------|------|------|
| `data/xxx.csv` | 读/写/新建 | [用途] |

**新字段/新表**（如有）：
| 列名 | 类型 | 默认值 | 说明 |
|------|------|--------|------|

---

## 5. 验收标准

> 操作 → 预期结果。逐条可 yes/no 判断。

1. [操作] → [预期结果]
2. [操作] → [预期结果]
3. [异常操作] → [预期错误反馈（不崩溃）]
